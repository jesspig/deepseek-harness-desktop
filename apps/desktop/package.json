{
  "name": "@dsh-desktop/app",
  "version": "0.1.1",
  "private": true,
  "description": "DeepSeek Harness desktop shell",
  "homepage": "https://github.com/jesspig/deepseek-harness-desktop",
  "main": "dist/main.js",
  "type": "commonjs",
  "scripts": {
    "dev": "pnpm build && electron .",
    "dev:watch": "tsc -p tsconfig.json --watch",
    "build": "tsc -p tsconfig.json",
    "dist": "pnpm build && node scripts/pack-dsh.mjs && electron-builder --config electron-builder.yml"
  },
  "devDependencies": {
    "@dsh-desktop/bundle": "workspace:*",
    "@dsh-desktop/client": "workspace:*",
    "@deepseek-ai/dsh": "0.1.0-rc.6",
    "electron": "43.4.0",
    "electron-builder": "^26.0.0",
    "typescript": "^5.9.3"
  }
}
