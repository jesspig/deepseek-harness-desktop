# 更新日志(摘要)

- 2026-08-16 启动加速实施完成(feature/startup-acceleration):窗口先行(双击 <100ms 出现 splash)、启动打点四段、NODE_COMPILE_CACHE、关窗隐藏 + 托盘常驻、退出无残留;pack-dsh.mjs 资源瘦身(245.4MB/32756 文件 → 107.9MB/12195 文件);实测:二次启动后端 1.7s、前端 454ms、关窗常驻与单例锁通过;冷启动大头为 Defender 扫描与页缓存冷读;v8CacheOptions 实验无显著差异保持默认。同日修正双加载界面问题:Electron splash 由加载界面简化为静态品牌启动画面(删除 spinner/文案/状态机制),加载反馈由 Web UI 自带加载页承担,窗口秒开保持(实测 27–33ms)。品牌页进一步升级:官方 dsh 文字 logo(SVG,提取自 BrandWordmark 组件)+ prefers-color-scheme 深浅色主题适配;exe 与窗口标题栏接入品牌图标(官方 favicon 派生品牌蓝);修复托盘退出误弹错误弹窗(quitting 守卫补全);本地打包注入项目 tag 版本 0.1.0(Setup 0.1.0.exe);修复可复现构建 Issue:重新实现 desktop-client(会话头部打开工作区目录按钮,经官方 host.openPath,canOpenPath 门控)与 desktop-bundle(最小 host 插件),模板绝对路径改相对路径,根构建串联插件包,dsh.client 接线(Loader 注册,client.js 实测 200);干净构建全链路通过、秒开保持;新增 pnpm package 打包命令,产物输出目录改为仓库根 build/release(CI 路径同步);修复 client 注册协议(esbuild 生成 __ModuleLoader__.load 包装)与 profile 链接目标更新(realpath 比较自动重建),Failed to load plugins 问题解决。明细见 [changelog/2026-08-16-log.md](changelog/2026-08-16-log.md)。
- 2026-08-15 新增 GitHub Actions 三平台自动打包(`.github/workflows/release.yml`):tag 触发(兼容 `vX.Y.Z` 与 `X.Y.Z`),Windows/Linux/macOS(x64+arm64)四 job 并行构建并汇总发布 GitHub Release;新增 `apps/desktop/scripts/set-version.mjs` 注入 tag 版本,`electron-builder.yml` 补充 linux(AppImage+deb)/mac(dmg)目标。M0 薄壳实施完成并通过验收:实现 `apps/desktop/`(Electron 入口:spawn dsh 子进程、URL 行解析、窗口、生命周期、单例、日志、profile 引导;打包脚本 `generate-profile.mjs` / `pack-dsh.mjs`;NSIS 打包)、`packages/desktop-bundle/`(最小 host 插件,`dsh.bundle.patch` 声明)与 `packages/desktop-profile/template/`(官方 `dsh plugin add` 生成的 `[dsh-base, dsh-web-app, @dsh-desktop/bundle]` 模板)。实测验收:双击启动加载官方 Web UI、URL loopback 校验、关窗无残留、崩溃弹窗与日志、单例锁;新增 [upstream-behavior-verify.md](research/upstream-behavior-verify.md) 记录 URL 行/plugin add/Windows pnpm 11 实测结论。同日完成知识库与实现同步:改写 overview / milestones / implementation-plan / constraints / build-tooling 与四个模块页为已实现状态,清除全部"骨架初始化/未开始"过时描述,并按实现细节核对源码一致。明细见 [changelog/2026-08-15-log.md](changelog/2026-08-15-log.md)。
- 2026-08-14 初始化知识库,并写入详细实施方案:新建 `docs/wiki/`,收录项目概览、里程碑规划、硬约束、四个规划模块与工具链约定;基于上游源码与 npm 实况核实 profile/bundle/Web 启动协议,新增 [implementation-plan.md](project/implementation-plan.md),细化 M0 文件级步骤与验收标准。同日完成 [dsh-TUI 社区参考调研](research/dsh-tui-reference.md)(社区插件补位官方无 TUI 的形态验证、`dsh plugin add` 机制、发布模式与对桌面端的启示),并修正 desktop profile 的 bundles 组合为 `[dsh-base, dsh-web-app, desktop-bundle]`(官方 Web UI 依赖 `dsh-web-app` 提供 webServer 与 URL 就绪行)。明细见 [changelog/2026-08-14-log.md](changelog/2026-08-14-log.md)。