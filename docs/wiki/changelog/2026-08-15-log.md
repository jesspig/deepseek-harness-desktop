# 2026-08-15 更新日志

## 2026-08-15-00 M0 薄壳实施完成并通过验收

- 新建 `apps/desktop/`(Electron 入口):`src/` 含 main / preload / log / singleton / lifecycle / window / profile / dsh/process(契约见 `src/contracts.md`),`scripts/` 含 `generate-profile.mjs`(profile 模板生成)与 `pack-dsh.mjs`(dsh 依赖树打包复制),`electron-builder.yml`(NSIS,extraResources: dsh 依赖树 + profile 模板),`package.json` 精确锁定 `@deepseek-ai/dsh@0.1.0-rc.6`、electron 43.4.0。
- 新建 `packages/desktop-bundle/`(最小 host 插件包):`dsh.bundle.patch` 声明 + `cordis.patch.yml`(insert `desktop-host` 行)+ 插件仅记录加载日志。
- 新建 `packages/desktop-profile/template/`:由 `generate-profile.mjs` 经官方 `dsh plugin add` 机制生成,`bundles = [dsh-base, dsh-web-app, @dsh-desktop/bundle]`。
- 新增 [research/upstream-behavior-verify.md](../research/upstream-behavior-verify.md)(Research):T3 实测结论——URL 就绪行确切格式、`--port 0` 语义、`dsh plugin add` 的 bundle 层追加行为、Windows 下 pnpm 11 allowBuilds 占位符与网络并发崩溃的实测现象。
- 根工作区配置:`pnpm-workspace.yaml` 增加 `nodeLinker: hoisted` 与 `allowBuilds`(electron/koffi/node-pty 等原生依赖构建许可)。
- 关键实施结论(实测):ELECTRON_RUN_AS_NODE 模式下 `node-addon-require-builtin` 不可用(无 GetAlignedPointerFromEmbedderData),dsh 的 HMR 服务需 `--expose-internals` flag 前置;打包版 dsh 树放 extraResources(asar 内无法承载原生模块),profile 首启由 Electron 复制模板并建立 `profiles/node_modules` 链接。
- M0 验收(实测通过):双击启动 → 窗口加载官方 Web UI(HTTP 200);URL 行 loopback 校验;关窗无残留进程;数据落盘 DSH_HOME;子进程崩溃弹窗 + 日志可查;单例锁二次实例退出。唯一待用户验证项:配置 API Key 后的对话全链路。
- 更新 `index.md`(状态改为 M0 已完成)、`log.md`(摘要追加)。

## 2026-08-15-01 知识库与实现同步

- 重写 `project/overview.md`(Project):状态改为 M0 已实施完成并通过验收,列出实现资产与关键决策(包管理器 / dsh 版本锁定 / 打包形态)。
- 重写 `project/milestones.md`(Plan):M0 标记已完成并关联验收结果,M1+ 保持未开始规划。
- 重写 `project/implementation-plan.md`(Plan):从"尚未实施"改写为实施记录——上游核实结论保留,新增 M0 各步实施要点与验收结果,风险表补落地状态;M1+ 部分保持规划并标注。
- 重写 `project/constraints.md`(Policy):五项约束的核实状态全部更新为"已落实",附源码证据(URL 正则 / window.ts 安全配置 / 版本锁定值)。
- 重写 `config/build-tooling.md`(Convention):基于根 package.json / pnpm-workspace.yaml / lockfile 重写工具链事实(pnpm hoisted、Node ^22.19||>=24、electron 43.4.0、electron-builder ^26.0.0、dsh 0.1.0-rc.6),补充构建命令与打包流水线、运行时依赖解析约定。
- 重写 `modules/electron-host.md`(Module):从规划转述改为基于实际源码的模块结构表(src/ 各文件职责)、启动流程、打包模式差异与安全落实,resource 指向 `apps/desktop/src/main.ts`。
- 重写 `modules/desktop-bundle.md`(Module):记录实际包内容(cordis.patch.yml insert desktop-host、src/index.ts 占位日志),标注"占位"状态。
- 重写 `modules/desktop-profile.md`(Module):记录 template/ 三清单文件、generate-profile.mjs 四阶段生成流程与 src/profile.ts 运行时引导。
- 更新 `modules/desktop-client.md`(Module):明确"未实现,仅占位目录",保留规划待补充标注。
- 更新 `index.md`:模块章节去掉过时"(规划)"标记,按实现/占位/未实现标注;维护章节指向当日明细。
- 更新 `log.md`(同日合并)、根 `README.md` 与 `AGENTS.md` 的状态描述(骨架初始化 → M0 完成)。
- 一致性审计:逐页核对源码(`main.ts` / `dsh/process.ts` / `window.ts` / `lifecycle.ts` / `singleton.ts` / `log.ts` / `profile.ts` / `preload.ts` / `contracts.md`、两个打包脚本、electron-builder.yml、bundle 包、profile 模板),未发现页面与代码冲突;研究页(upstream-behavior-verify / dsh-tui-reference)结论与实现一致,未改动。

## 相关页面

- [更新摘要](../log.md)
- [目录入口](../index.md)
